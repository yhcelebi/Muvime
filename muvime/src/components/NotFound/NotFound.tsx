import { Console } from "console";
import "./NotFound.css";

export const NotFound = () => {
    return (
        <div className="container">
            <h1 className="not-found">{'Whoops, something bad happened'}</h1>
        </div>
    );
}